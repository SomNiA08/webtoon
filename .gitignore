# macOS
.DS_Store
.AppleDouble
.LSOverride

# 작업 산출물 (하네스 실행 시 생성되는 중간 결과물/렌더 이미지)
_workspace/
_workspace_*/
_archive/
RELEASE/
05_panels/
**/*.png
**/refs/

# 문서용 인포그래픽은 예외 (README 임베드)
!docs/images/
!docs/images/*.png

# 캐릭터 레퍼런스 시트는 예외 — 시리즈 외형 일관성의 SSOT 자산(유실 시 시리즈 닻 소실)
!04_visual/refs/
!04_visual/refs/*.png
!04_visual/refs/INDEX.md

# 사용자가 직접 만든 원본 캐릭터 이미지는 예외 — 기획의 입력 정본(재생성 불가)
!00_input/characters/
!00_input/characters/*.png

# 로그/임시
*.log
.cache/
node_modules/
